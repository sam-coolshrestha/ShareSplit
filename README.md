# ShareSplit (scaffold)

Repository scaffold for ShareSplit.

Next steps:

- Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` inside this folder to build the full app.
- Install runtime dependencies listed in the master prompt.
